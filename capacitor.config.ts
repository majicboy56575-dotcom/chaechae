import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.monopic.app',
  appName: 'Monopic',
  webDir: 'out',
  server: {
    url: 'https://chaechae--chae-chae.asia-east1.hosted.app',
    cleartext: false,
  },
};

export default config;
